# FastAPI Backend Requirements

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# WebSocket support
websockets>=12.0

# CORS (provided by Starlette/FastAPI)
# No additional package needed - use starlette.middleware.cors

# Validation
pydantic>=2.0.0

# Async support
aiofiles>=23.2.0

# Core dependencies (shared with main project)
numpy>=1.24.0

# Authentication
supabase>=2.0.0
pyjwt>=2.8.0

# Image processing
Pillow>=10.0.0

# Docker simulation support
docker>=7.0.0  # Docker Python SDK for Gazebo container management

# NeRD (Neural Robot Dynamics) support
h5py>=3.10.0  # HDF5 data storage for training datasets
roslibpy>=1.5.0  # ROS Bridge Python client for Gazebo data collection
